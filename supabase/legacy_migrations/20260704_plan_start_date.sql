-- ============================================================================
-- 学習開始日（ロードマップの経過日数の基点）
-- ----------------------------------------------------------------------------
-- 「本来いるべきフェーズ」を試験日と学習開始日からの経過日数で算出するため、
-- 学習を始めた日を記録する。オンボーディング完了時に一度だけセットし、以後は
-- 変更しない（既存ユーザーは初回の /plan 表示時に当日で補完＝実質「今日から」）。
-- アクセスは既存方針どおり service role 経由の API Route に限定。
-- ============================================================================
alter table public.user_profiles add column if not exists plan_start_date date;
