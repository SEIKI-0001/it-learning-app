-- ============================================================================
-- 今週のタスクリスト（週次スナップショット）
-- ----------------------------------------------------------------------------
-- 「今週やること」を週初め（月曜）に一度確定して保存し、週の途中で内容が
-- 入れ替わらないようにする。チェック状態は保存せず completed_topics 等から
-- 都度導出するため、確定リスト（週の開始日 + トピックid/復習id）のみを持つ。
-- 可変長のため jsonb でまるごと保存する（1ユーザー1行の user_progress に追加）。
-- アクセスは既存方針どおり service role 経由の API Route に限定。
-- ============================================================================
alter table public.user_progress add column if not exists weekly_plan jsonb;
