# ゲームフルデザインv2 実機QA結果（2026-09-05）

手順書: `docs/qa/gameful-v2-manual-qa.md`
対象コミット: `281a024`（`origin/main`。PR #33 マージ後 + exam-readiness 統合後）
実行環境: production build（`next build` + `next start`）/ Chromium / 375・390・430px /
`prefers-reduced-motion: reduce` 併走 / **未ログイン（匿名）**

## 0. 結論

**`VERIFIED` へは昇格させない。** 重大度「高」が2件あり、§9 完了条件1を満たさない。

- `GF-P1-011`（用語統一）は §7 の照合で**廃止表記0件**。この要件単体の受け入れ基準は満たした。
- ただし `GF-P0-003`（成長確認）は画面が表示できず、`GF-P1-003`（CP突破演出）は
  未ログインで到達できないため、v2全体としては未達。

## 1. 重大度「高」

### H-1. `/growth-check` が永久に「読み込み中…」— GF-P0-003 の画面が表示できない

```
[ジャーニー] J5
[端末/幅]   Chromium / 390px（幅非依存）
[事前状態]  オンボーディング直後／1レッスン完了後のいずれでも再現
[手順]      /growth-check へ直接アクセス、またはリロード
[観測]      LoadingScreen「読み込み中…」から永久に進まない
[期待]      成長の根拠一覧、または任意ミニ確認が表示される
[観点]      A / E
[重大度]    高（機能全体が到達不能）
[原因]      app/growth-check/page.tsx の `useState(() => {...})` 初期化関数が
            state 未読込（undefined）の初回レンダーで実行され、view が null に固定される。
            useAppState は必ず undefined から始まり effect で値を入れるため、
            初期化関数が再実行されることはない。
            結果 `if (state === undefined || state === null || view === null)` が
            恒真になり LoadingScreen を返し続ける。
```

`/today` のカードからのクライアント遷移でも同じ（コンポーネントは新規マウントされ、
初回レンダーの state は undefined のため）。

**既存テストが素通りした理由**: `test/GrowthCheckPage.test.tsx` は `useAppState` を
モックして state を最初から与えるため、初回レンダーが undefined になる実挙動を再現しない。
回帰テストは「state が undefined の初回レンダーを経てから表示される」形にする必要がある。

### H-2. 未ログインで突破試験を完遂できない（J7→J8 が通らない）

```
[ジャーニー] J7 / J8
[事前状態]  CP1 の解放条件を満たし、出題に足るトピックを完了済み・未ログイン
[手順]      /checkpoint/cp1/final →「突破試験に挑む」→ 全6問に解答
[観測]      ① 開始時:「評価セッションを開始できませんでした。もう一度お試しください。」
            ② ①を迂回して解答完了後:「採点結果を保存できませんでした。もう一度お試しください。」
               → 合否が表示されず、CPも進まない
[期待]      未ログインでもローカル完結で合否が出る（§6 非機能「未ログイン」）
[観点]      A / E
[重大度]    高（CP進行が不能）
[失敗API]   401 POST /api/assessment-sessions
            401 POST /api/question-attempts/save
```

**回帰の所在**: PR #33 のマージ時点（`e7cbf84`）の同ページは
`saveQuestionAttemptsForCurrentSession` を使っており、この関数は 401 を
`authState:"anonymous"` として**graceful に扱い例外を投げない**。`uid` が null なら
DB保存を飛ばすだけで、採点と CP 進行はローカルで成立していた。
現行 main は exam-readiness 統合で、サーバー永続化の成功を採点結果表示の前提に
変更した（`Assessment finalization was not fully persisted` を throw → catch で
persistenceError 表示）。同じ前提は `/mock-exam`・過去問・テーマ別試験・
分野まとめテストの各 Runner にも入っている（同一文言が6箇所）。

**未検証**: ログイン済みユーザーで成功するかは、ローカルに Supabase の
環境変数が無いため確認できていない。本番相当環境での再確認が必要。
なお `401 GET /api/session/state` と `401 GET /api/exam-readiness/current` は
画面を壊さず、これらは問題ない。

## 2. 重大度「中」

### M-1. `/today` に同一の Primary CTA が2つ並ぶ

```
[ジャーニー] J2
[観測]      「今日の最優先」カードの「学習を始める」(y=439) と、
            「今日のルート」の現在ノードの「学習を始める」(y=755) が
            リンク先・ラベル・強調色(rgb(8,104,201))まで完全に同一。
            トピック名・所要時間・種別も両方に出る。
[期待]      Primary は1つ。ルート側は現在地の表示に留める等
[観点]      B / D
[重大度]    中（迷いと重複。GF-P0-001「重複表示整理」の趣旨に反する）
```

### M-2. 復帰導線の分量が要件の3〜5分に対して約15分

```
[ジャーニー] J12
[事前状態]  progress.lastPlayedAt を5日前に設定
[観測]      「5日ぶりです。まずは前に学んだところを、約15分だけ思い出すところから戻りましょう。」
            （2件 × 約8分 + 約7分）
[期待]      GF-P1-002「3〜5分の復帰ルートを提示する」
[観点]      C
[重大度]    中（「軽い再開」と言いながら通常ルート並みの分量）
```

導線そのもの（見出し「おかえりなさい」／責める文言なし／通常導線へ進める／
ルートより先に出る／UIに「ミッション」を出さない）はすべて期待どおり。
問題は `COMEBACK_MISSION_SIZE = 2` の件数固定で、所要時間の上限を見ていない点。

## 3. 重大度「低」

### L-1. 「今日の3ミッション」の中身が折りたたみの中だけにある

`/today` の可視領域には宝箱の「今日の3ミッション達成で開けられる」しか出ず、
3件の内容・進捗・差し替え（`GF-P1-009`）は `<details>`「今日の状況」を開かないと見えない。
達成条件を示されたユーザーが、その条件を同じ画面で確認できない（観点A）。

### L-2. バッジ獲得トーストが突破試験の問題文に重なる

出題中の画面中央にトーストが縦に積まれ、設問と選択肢を覆った（観点C）。
QAでは seed により5件同時獲得させたため増幅されている。通常プレイでの同時獲得は
1〜2件と見込まれるが、表示位置が本文の上である点は変わらない。

### L-3. 学習量「5分」でルートが約8分

最小の学習単位が8分のため、5分を選んでも約8分の1件になる。
「意味のある完了単位が存在する」（受け入れ基準）は満たすが、
「選択時間内に収まる」（要件本文）は満たさない。ヘッダの「1件・約8分」は正直な表示。

## 4. 合格した項目

| 区分 | 内容 |
|---|---|
| `GF-P1-011` | 11画面 × 廃止表記7種の全走査で**0件**。「突破試験」「今日の3ミッション」「かけら」「いま挑戦中のチェックポイント」「分野まとめテスト」に統一されている |
| 数値整合 | 突破試験の問題数・合格ラインが `/plan` と `/checkpoint/cp1/final` で一致（6問中4問） |
| J1 | 確認画面を挟まず `/today` へ直行。ナビ5件。試験日「未設定」を正直に表示。ファーストビュー3ブロック |
| J2 | 「これが進みます」を開始前に2件提示（必須バッジ条件・測定データ）。予告と結果が矛盾しない |
| J3 | 完了後に「このレッスン、おつかれさま！／4問中0問正解／今回の変化／このトピックの理解度」が出る（`GF-P0-005` 成立） |
| J6 | 誤答後の Primary が新規学習から「弱点」へ切り替わる。`/review` が間違い/苦手/予定に分類 |
| J9 | 宝箱の進捗が `0/3 達成`。`/rewards` の所持・交換コストが「ノーマルのかけら」で一致 |
| J13 | 5分→1件約8分 / 15分→1件約8分 / 30分→4件約29分 / おまかせ→2件約16分。選択は当日保存され、おまかせで解除される。長い選択肢を優遇しない |
| J14 | 合格宣言が任意である旨と取り消し可能である旨を明記 |
| 非機能 | 375/390/430px × 11画面で横スクロール0件 |
| 非機能 | reduced-motion で `/today` `/plan` `/rewards` `/badges` の演出が停止し、情報は残る |
| 非機能 | Primary CTA が `<a>` として露出（役割が読み上げ可能） |

## 5. 実施できていない項目

| 項目 | 理由 |
|---|---|
| iPhone/Android 実機（Safari・Chrome） | 本QAは Chromium のみ。実機の描画・タップ領域は未確認 |
| ログイン済み（Google / LINE）の3系統 | ローカルに Supabase 環境変数が無く、認証経路を通せない。**H-2 の切り分けに必須** |
| 端末間マージ | 同上 |
| J4 バッジ獲得の自然発生 | seed による一括獲得で代替（L-2 はその過程で観測） |
| J5 の中身 | H-1 により画面自体が表示できないため未検証 |
| J8 の CP突破演出・モチット成長 | H-2 により採点まで到達できないため未検証 |
| J10 ヒートマップの粒度 | 履歴が1日分しかなく、複数日の粒度は未検証 |
| J11 図鑑の収集状況 | 単語帳未消化（103語すべて未学習）の状態しか見ていない |

## 6. 次のアクション（提案）

1. H-1 を修正する（`useState` 初期化 → state 確定後に `useMemo`/effect で確定）。
   併せて「初回レンダーが undefined」を再現する回帰テストを足す。
2. H-2 の切り分け: 本番相当環境でログイン済みユーザーの突破試験を通す。
   未ログインを引き続き支えるなら、採点表示をサーバー永続化から切り離す。
3. M-1・M-2 を判断（仕様を変えるか、実装を寄せるか）。
4. 上記を解消してから J5・J8・J10・J11 を再実行し、実機（iPhone/Android）で1周する。
5. すべて満たしてから §9 に従って Tracker を `VERIFIED` へ更新する。

---

# 再QA #1（PR-A 適用後・2026-09-05）

対象: `fix/gameful-qa-blockers`（PR-A）。上の「初回QA」は当時の記録として残す。
環境は初回と同じ（production build / Chromium / 390px / 未ログイン）。

## 検証した項目

| 項目 | 初回 | 再QA | 結果 |
|---|---|---|---|
| H-1 `/growth-check` 直接アクセス | 読み込み中…（永久） | 「ここまでの成長」が描画 | **解消** |
| H-1 リロード | 読み込み中…（永久） | 同上 | **解消** |
| H-1 「比較材料なし」と「読み込み中」の区別 | 区別なし（常に Loading） | 材料なしのとき「まだ比べられる記録がありません」を表示 | **解消** |
| M-1 `/today` の Primary CTA 数 | 2（同一href・同一ラベル・同一色） | **1**（ルート側は「上の『今日の最優先』から始められます」） | **解消** |
| M-1 ルートの現在地把握 | — | 「いま挑戦中」マーカーと全体ルートは維持 | 維持 |
| M-2 復帰導線の所要時間（予算内の単位あり） | 約15分 | **約5分**（LANとWAN） | **解消** |
| M-2 復帰導線の所要時間（fallback） | 約15分 | 約8分（完了トピックが8分1件のみ＝最短1件） | 定義済みfallback |
| M-2 「責めない」「通常導線へ進める」 | OK | OK（変化なし） | 回帰なし |

## H-2 について

PR-A では未着手。未ログインでの突破試験は依然として完遂できない。PR-B で対応する。

## 自動テスト

`149 files / 1854 tests` すべて通過。lint・typecheck・build も通過。

追加した回帰テスト:

- `test/GrowthCheckPage.test.tsx` … 「初回レンダーは `useAppState() === undefined`
  → rerender で AppState がロード」の2フェーズを再現する4件。
  **修正前の実装に対して4件とも失敗することを確認済み**（モックが最初から
  AppState を返す既存テストではこの経路を踏めない）。
- `test/QuestRouteComponent.test.tsx` … Primary と同一 href のときに開始ボタンを
  出さない／別の行動なら出す／未指定なら出す／抑止時も記述問題の導線は残す、の4件。
- `test/comebackMission.test.ts` … 予算内に収まるときは合計が上限以内／収まる単位が
  無ければ最短1件へ fallback／fallback では短さが古さに優先／復帰直後に
  通常の学習量を積まない、の4件。

## 付随して直したもの（PR-A の A1〜A3 とは別）

`e2e/floating-mochit.spec.ts` の E2E が **main（`281a024`）の時点で既に落ちていた**。
`getByRole("menuitem", { name: "今日の学習" })` が部分一致のため、
モチットの吹き出しに出る状況依存の導線「今日の学習を見る」にも解決してしまい
strict mode violation になる。`e7cbf84`（PR #33）では緑で、exam-readiness 統合
（`281a024`）以降に赤いままだった。

名前を完全一致（`exact: true`）で引くよう修正した。テストの削除・無効化はしていない。
PR-A の差分が原因ではないが、PR-A のマージを塞ぐため同PRに含めた。

## 残課題（PR-A の対象外）

- H-2（PR-B）
- L-1 3ミッションが折りたたみ内だけ
- L-2 バッジトーストが設問に重なる
- L-3 学習量5分の選択で約8分になる

L-1〜L-3 は backlog として据え置き。PR-A / PR-B 後の再QAで重大度を再評価する。

---

# 再QA #2（PR-A・PR-B マージ後・2026-09-05）

対象: `d04588a`（`origin/main`。PR #34 と PR #35 をマージ済み）。
環境は初回と同じ（production build / Chromium / 375・390・430px / **未ログイン**）。
初回QA・再QA #1 の記録は上書きせず、本セクションを追記した。

## 1. VERIFIED 昇格条件の確認

| 条件 | 結果 | 実測 |
|---|---|---|
| H-1 解消 | **OK** | `/growth-check` は直接アクセス・リロードとも「ここまでの成長」を描画 |
| H-2 解消 | **OK** | 未ログインで突破試験を開始・回答・採点・結果表示まで完遂 |
| J5 完遂 | **OK** | 「学習記録からの変化 / 以前まちがえた問題に、いまは正解できます（3問）」を表示 |
| J7 → J8 完遂 | **OK** | 「CP1 を突破しました / 6問中 6問正解」。ローカル進行も `cur: cp1→cp2` / `cleared: [cp0]→[cp0,cp1]` / `finalExamAttempts` に合格1件 |
| Primary CTA 重複解消 | **OK** | ファーストビューの Primary色CTA は **1個**。ルート側は「上の『今日の最優先』から始められます」 |
| 復帰 3〜5分 | **OK** | 実測 **5分**。責める文言なし・ルートより先・UIに「ミッション」表記なし |
| 既存成功項目に回帰なし | **OK** | 下記 §2 |

## 2. 回帰確認

| 項目 | 結果 |
|---|---|
| 用語（§7 の廃止表記7種 × 11画面） | **0件** |
| 横スクロール（375 / 390 / 430px × 11画面） | **0件** |
| J1 ナビ5件 | OK |
| J2 「これが進みます」の事前提示 | OK |
| J3 学習完了の成果差分（今回の変化・理解度） | OK |
| J6 復習優先（Primary が「弱点」に切り替わる / `/review` の分類） | OK |
| J9 宝箱の進捗 `n/3 達成` | OK |
| J13 学習量 | おまかせ 2件・約16分 / 5分 1件・約8分 / 15分 1件・約8分 / 30分 4件・約29分。選択は `aria-pressed` に反映 |
| reduced-motion（`/today` `/plan` `/rewards` `/badges`） | アニメ0件・情報は残る |
| 他の評価画面（未ログイン）: 分野まとめテスト / 模試 / テーマ別試験 / 過去問 | 4画面すべて エラーなし・結果表示あり |
| 自動テスト | `150 files / 1866 tests` 通過。lint / typecheck / build も通過 |

## 3. 昇格した要件

§9 の完了条件を満たしたため、下記を `VERIFIED` へ更新した。

`GF-P0-001` `GF-P0-002` `GF-P0-003` `GF-P0-004` `GF-P0-005`
`GF-P1-001` `GF-P1-002` `GF-P1-003` `GF-P1-004` `GF-P1-005`
`GF-P1-006` `GF-P1-007` `GF-P1-008` `GF-P1-009` `GF-P1-010` `GF-P1-011`

`GF-P0-006`（LINE通知）は未実装のため対象外（`NOT_STARTED` のまま）。

## 4. 昇格の限界（この QA で確認できていないこと）

`VERIFIED` は **未ログイン・Chromium・production build** での確認に基づく。以下は未確認のまま残る。

- iPhone / Android の実機（Safari / Chrome）。描画とタップ領域は未確認
- **ログイン済み（Google / LINE）経路**。ローカルに Supabase の環境変数が無く認証を通せない。
  strict assessment の回帰は自動テストで確認しているが、実機確認は別途必要
- 端末間マージ（同上）
- J10 ヒートマップの複数日粒度（履歴が浅い）
- J11 単語帳の図鑑（103語すべて未学習の状態しか見ていない）

## 5. QA backlog（重大度を再評価した結果）

| ID | 内容 | 再評価 |
|---|---|---|
| L-1 | 今日の3ミッションの中身が「今日の状況」折りたたみ内だけ | **低のまま**。宝箱が達成条件を示すのに中身が同画面で見えない導線の弱さは残る |
| L-2 | バッジ獲得トーストが突破試験の設問に重なる | **低のまま**。QAでは seed による同時5件獲得で増幅。表示位置が本文上である点は未変更 |
| L-3 | 学習量5分の選択で約8分になる | **低のまま**。最小の学習単位が8分のため。ヘッダは「1件・約8分」と正直に出る |

いずれも blocker ではない。次の改善サイクルで扱う。
